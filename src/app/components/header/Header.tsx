import { Pattern, Logo, Title, FeedbackForm } from "../../../lib/common.imports";

export default function Header() {
    return (
        <header>
            <Pattern />
            <Logo />
            <Title />
            <FeedbackForm />
        </header>
    );
}
