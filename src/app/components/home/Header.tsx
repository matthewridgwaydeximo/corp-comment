import { Pattern, Logo, PageHeading, FeedbackForm } from "../../../lib/home.imports";

export default function Header() {
    return (
        <header>
            <Pattern />
            <Logo />
            <PageHeading />
            <FeedbackForm />
        </header>
    );
}
